<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.realtime_production_data">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="com.android.hardware.camera.focus" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ScanActivity"></activity>
        <activity android:name=".GenerateCode" />
        <activity android:name=".GenerateQRCode" />
        <activity android:name=".Support" />
        <activity android:name=".MaintainanceData" />
        <activity android:name=".InventoryData" />
        <activity android:name=".graphicalrepresentation" />
        <activity android:name=".Machine_Line_Two_Data" />
        <activity android:name=".MachineLineOneData" />
        <activity android:name=".Dashboard" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.gms.vision.DEPENDENCIES"
            android:value="barcode" />
    </application>

</manifest>